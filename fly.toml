app = 'anker-solix'
primary_region = 'fra'

[build]

[http_service]
  internal_port = 8420
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

[[mounts]]
  source = 'solar_data'
  destination = '/app/data'
